# Shared default: deployed Render backend.
EXPO_PUBLIC_API_BASE_URL=https://newbiethon-2026.onrender.com

# For local backend development, replace this with localhost (web/iOS simulator)
# or the development computer's LAN IP address (physical phone).
